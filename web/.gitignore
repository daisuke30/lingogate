node_modules/
dist/
# Generated at build time from pipeline JSONL (predev/prebuild). Not source.
# deck.generated.json = legacy name (pre-LINGO-014); deck.<courseId>.json = per-course packs.
src/content/deck.generated.json
src/content/deck.*.json
# Generated at build time (predev/prebuild) — git sha + timestamp stamp. Not source.
src/content/version.generated.json
public/icons/
*.local
.DS_Store
# Screenshots from scripts/ui-audit.mjs (LINGO-042) — ephemeral evidence for
# the human running the gate, not source control artifacts. Default dir is
# ui-audit-out/; a custom dir (e.g. ui-audit-prod/) can be passed as argv[2].
ui-audit-out/
ui-audit-prod/
